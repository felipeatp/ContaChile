# Alertas de Vencimiento — Implementation Plan (Módulo 3)

> Ejecutar tarea por tarea. Un commit por tarea.

---

## Task 1: Constantes + helper de fechas

- [ ] Crear `packages/validators/src/vencimientos.ts` con:
  - `VencimientoConfig` interface
  - `VENCIMIENTOS_MENSUALES` array (cotizaciones 10, retención 12, F29 20)
  - `UpcomingAlert` interface
  - `adjustForWeekend(date)`: sábado → +2 días, domingo → +1 día
  - `findUpcomingDueDates(today, monthsAhead = 1)`: itera meses, calcula fecha ajustada y `daysUntil`
- [ ] Exportar desde `packages/validators/src/index.ts`
- [ ] Build validators
- [ ] Commit: `feat(validators): add vencimientos calendar and weekend rollover helper`

---

## Task 2: Schema Prisma AlertSent

- [ ] Agregar al schema:

```prisma
model AlertSent {
  id         String   @id @default(cuid())
  companyId  String
  alertCode  String
  dueDate    DateTime
  daysBefore Int
  sentAt     DateTime @default(now())

  @@unique([companyId, alertCode, dueDate, daysBefore])
  @@index([companyId])
  @@index([sentAt])
}
```

- [ ] `prisma db push`
- [ ] Commit: `feat(db): add AlertSent model for dedup`

---

## Task 3: Extender email service

- [ ] En `apps/api/src/lib/email.ts`:
  - Agregar tipo `SendDueAlertParams`
  - Agregar método `sendDueAlert(params)` en interface y en `StubEmailService`+`ResendEmailService`
  - Template HTML con label, fecha, daysUntil, link
- [ ] Commit: `feat(api): extend email service with sendDueAlert`

---

## Task 4: API endpoint /alerts/upcoming

- [ ] Crear `apps/api/src/routes/alerts.ts` con GET /alerts/upcoming
- [ ] Registrar en index.ts
- [ ] Commit: `feat(api): add alerts/upcoming endpoint`

---

## Task 5: Worker BullMQ alerts-daily

- [ ] Crear `apps/api/src/workers/alerts.ts`:
  - Conecta a Redis con fallback graceful
  - Queue scheduled `0 8 * * *` (08:00 diario)
  - Procesa: itera empresas, calcula próximos vencimientos, envía email + AlertSent para daysUntil ∈ {5,1}
- [ ] Importar desde `apps/api/src/index.ts`
- [ ] Commit: `feat(api): add daily alerts worker`

---

## Task 6: Banner UI

- [ ] Crear proxy `apps/web/app/api/alerts/upcoming/route.ts`
- [ ] Crear componente `apps/web/components/dashboard/upcoming-alerts-banner.tsx`:
  - Fetch /api/alerts/upcoming
  - Mostrar lista (rojo si vencido, amarillo si ≤5 días)
  - Link por cada alerta
- [ ] Reemplazar `<F29Alert />` en dashboard con `<UpcomingAlertsBanner />`
- [ ] Build web
- [ ] Commit: `feat(web): replace F29Alert with generic upcoming alerts banner`

---

## Task 7: Smoke test

- [ ] Crear `apps/api/scripts/smoke-alerts.ts`:
  - Verificar `findUpcomingDueDates` con fecha conocida
  - Verificar weekend rollover (mayo 10 2026 = domingo)
  - Test dedup: insertar AlertSent, llamar segunda vez no debe duplicar
- [ ] Correr y commitear si pasa
