# Remuneraciones — Implementation Plan (Módulo 2)

> Ejecutar tarea por tarea. Cada tarea termina con un commit. Marcar `[x]` al avanzar.

**Goal:** Implementar Módulo 2 (Trabajadores + Liquidaciones + Exportaciones).

**Tech Stack:** Prisma, Fastify, Next.js 14, Zod, pdfkit.

---

## Sub-módulo 2A: Trabajadores

### Task 1: Prisma schema + db push

- [ ] Agregar al final de `packages/db/prisma/schema.prisma`:

```prisma
enum ContractType {
  INDEFINIDO
  PLAZO_FIJO
  HONORARIOS
}

enum AfpCode {
  CAPITAL
  CUPRUM
  HABITAT
  MODELO
  PLANVITAL
  PROVIDA
  UNO
}

enum HealthPlan {
  FONASA
  ISAPRE
}

enum PayrollStatus {
  DRAFT
  APPROVED
  PAID
}

model Employee {
  id           String       @id @default(cuid())
  companyId    String
  rut          String
  name         String
  email        String?
  position     String
  startDate    DateTime
  endDate      DateTime?
  contractType ContractType
  workHours    Int          @default(45)
  baseSalary   Int
  afp          AfpCode
  healthPlan   HealthPlan
  healthAmount Int?
  isActive     Boolean      @default(true)
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  payrolls     Payroll[]

  @@unique([companyId, rut])
  @@index([companyId])
  @@index([isActive])
}

model Payroll {
  id            String        @id @default(cuid())
  companyId     String
  employeeId    String
  year          Int
  month         Int
  bruto         Int
  horasExtras   Int           @default(0)
  bonos         Int           @default(0)
  afp           Int
  salud         Int
  cesantia      Int
  baseImponible Int
  impuesto      Int
  otrosDesc     Int           @default(0)
  liquido       Int
  status        PayrollStatus @default(DRAFT)
  generatedAt   DateTime      @default(now())
  approvedAt    DateTime?
  paidAt        DateTime?
  employee      Employee      @relation(fields: [employeeId], references: [id])

  @@unique([companyId, employeeId, year, month])
  @@index([companyId, year, month])
  @@index([status])
}
```

- [ ] Regenerar: `pnpm --filter @contachile/db db:generate`
- [ ] Push schema: `prisma db push` con DATABASE_URL
- [ ] Commit: `feat(db): add Employee and Payroll models`

---

### Task 2: Payroll constants + calcularLiquidacion

- [ ] Crear `packages/validators/src/payroll-constants.ts` con AFP_RATES, SALUD_FONASA, SEGURO_CESANTIA_*, UTM_DEFAULT, TAX_BRACKETS.
- [ ] Crear `packages/validators/src/payroll.ts` con `calcularLiquidacion` y `calcularImpuestoUnico` (tabla progresiva con rebajas en UTM).
- [ ] Crear Zod schemas: `CreateEmployeeSchema`, `UpdateEmployeeSchema`, `GeneratePayrollSchema`.
- [ ] Exportar todo desde `packages/validators/src/index.ts`.
- [ ] Build validators.
- [ ] Commit: `feat(validators): add payroll constants, calculator and schemas`

---

### Task 3: Employee CRUD routes

- [ ] Crear `apps/api/src/routes/employees.ts` con GET/POST/PATCH/DELETE (soft delete).
- [ ] Validar RUT con `validateRUT` existente y `companyId+rut` único.
- [ ] Registrar ruta en `apps/api/src/index.ts`.
- [ ] Commit: `feat(api): add employees CRUD endpoints`

---

### Task 4: Employee UI

- [ ] Crear proxy `apps/web/app/api/employees/route.ts` y `.../[id]/route.ts`.
- [ ] Crear `apps/web/app/remuneraciones/trabajadores/page.tsx`: tabla + modal form (alta/edición).
- [ ] Sidebar nav: agregar entrada "Trabajadores" bajo grupo Remuneraciones.
- [ ] Build web.
- [ ] Commit: `feat(web): add employees management page`

---

## Sub-módulo 2B: Liquidaciones

### Task 5: Payroll calculator + generate endpoint

- [ ] Crear `apps/api/src/lib/payroll-service.ts` con `generatePayrollForMonth(companyId, year, month)`:
  - Lista empleados activos (no honorarios, sin liquidación ese mes)
  - Llama `calcularLiquidacion` para cada uno
  - Inserta en `Payroll`
- [ ] Crear `apps/api/src/routes/payroll.ts` con:
  - POST `/payroll/generate` body `{year, month}`
  - GET `/payroll/:year/:month`
  - GET `/payroll/item/:id`
  - POST `/payroll/item/:id/approve` (crea asiento en libro diario)
- [ ] Helper `createPayrollEntry` en `apps/api/src/lib/accounting-entries.ts`:
  - Débito: 5100 Gastos personal por bruto
  - Crédito: 2115 Remuneraciones por pagar por líquido
  - Crédito: 2110 Impuestos por impuesto + cotizaciones
- [ ] Registrar ruta en index.
- [ ] Commit: `feat(api): add payroll generation and approval endpoints`

---

### Task 6: PDF de liquidación

- [ ] Crear `apps/api/src/lib/payroll-pdf.ts` con `generatePayrollPdf(payroll, employee, company)` usando pdfkit.
- [ ] Endpoint GET `/payroll/item/:id/pdf` que devuelve `application/pdf`.
- [ ] Commit: `feat(api): add payroll PDF generation`

---

### Task 7: UI liquidaciones

- [ ] Proxies Next.js para `/payroll/*`.
- [ ] Crear `apps/web/app/remuneraciones/liquidaciones/page.tsx`: selector mes/año, botón "Generar", lista, vista detalle, descarga PDF, botón aprobar.
- [ ] Sidebar nav: "Liquidaciones".
- [ ] Build web.
- [ ] Commit: `feat(web): add payroll page`

---

## Sub-módulo 2C: Exportaciones

### Task 8: PreviRed export

- [ ] Crear `apps/api/src/lib/previred-export.ts` con función que recibe lista de Payroll+Employee y devuelve string CSV con encabezado y filas.
- [ ] Endpoint GET `/payroll/previred/:year/:month` que retorna `text/plain` con header `Content-Disposition: attachment; filename="previred_AAAAMM_RUT.txt"`.
- [ ] Commit: `feat(api): add PreviRed export endpoint`

---

### Task 9: DDJJ 1887 placeholder

- [ ] Endpoint GET `/payroll/ddjj-1887/:year` que retorna CSV simple por ahora (RUT empleado, total sueldos del año).
- [ ] Commit: `feat(api): add DDJJ 1887 placeholder export`

---

### Task 10: UI exportaciones

- [ ] Crear `apps/web/app/remuneraciones/exportaciones/page.tsx` con botones de descarga.
- [ ] Sidebar nav: "Exportaciones".
- [ ] Build web.
- [ ] Commit: `feat(web): add payroll exports page`

---

## Task 11: Smoke test

- [ ] Crear `apps/api/scripts/smoke-payroll.ts` que:
  - Crea empleado $1M sueldo (impuesto = 0)
  - Crea empleado $3M sueldo (impuesto > 0)
  - Genera liquidaciones del mes
  - Verifica cálculos manualmente
  - Aprueba una liquidación y verifica asiento creado
  - Genera archivo PreviRed y valida formato
- [ ] Correr y commitear si pasa.

---

## Spec Coverage Check

| Spec | Tarea |
|---|---|
| Employee schema | 1 |
| Payroll schema | 1 |
| Tasas AFP, salud, cesantía | 2 |
| Impuesto único progresivo | 2 |
| calcularLiquidacion | 2 |
| CRUD empleados | 3, 4 |
| Generate payroll del mes | 5 |
| Asiento contable automático al aprobar | 5 |
| PDF liquidación | 6 |
| UI liquidaciones | 7 |
| PreviRed export | 8 |
| DDJJ 1887 | 9 |
| UI exportaciones | 10 |
| Smoke test cubriendo cálculos | 11 |
